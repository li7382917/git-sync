[project]
name = "git-sync"
version = "0.1.0"
description = "Auto-sync local projects to GitHub — create repo if missing, then push"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
